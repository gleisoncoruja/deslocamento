import styled from "@emotion/styled";
import { Table, TableContainer } from "@mui/material";

export const StyledTableContent = styled(TableContainer)({
  width: "100%",
  overflow: "auto",
});
